# Contributing

PRs welcome. Before opening one:

```
npm install
npm run typecheck
npm run lint
npm test
```

Keep PRs small and focused — one concern per PR. See `README.md` for
architecture and local setup.
